---
# ============================================================================
#  replicate-all.yml  (self-contained, Proxmox VE 9, dry-run-safe)
#
#  Enforces a UNIFORM HA policy on a Proxmox VE 9 cluster using local ZFS:
#    * every managed guest is replicated to EVERY other node
#    * every managed guest is HA-enabled (state=started)
#
#  IDEMPOTENCE IS PAIR-BASED, NOT ID-BASED. Proxmox renumbers replication
#  jobs when a guest migrates (direction-flip), so "does job id X exist"
#  is the wrong question. The right question is "does ANY job exist for
#  (guest -> target)". When a pair is missing, we pick a free job number
#  at runtime instead of assuming one.
#
#  PVE 9 note: HA groups were removed in favor of HA rules. An HA resource
#  with NO node-affinity rule can already run on ANY node -- exactly "runs
#  anywhere." So we simply enroll each guest as an HA resource.
#
#  Read-only lookups use check_mode:false so a --check (Dry Run) still
#  gathers real state and shows a meaningful plan while making ZERO changes.
#
#  Run against ONE node (pmxcfs is cluster-wide, so one node drives it all).
#  Idempotent + safe to re-run on a schedule so new guests self-heal in.
#
#  Usage:
#    ansible-playbook -i inventory.ini replicate-all.yml
#    ansible-playbook -i inventory.ini replicate-all.yml -e "guests=[100,101]"
#    ansible-playbook -i inventory.ini replicate-all.yml --check   # dry run
# ============================================================================

- name: Enforce replicate-all HA policy on Proxmox cluster
  hosts: pve_primary
  gather_facts: false
  become: true

  vars:
    replication_schedule: "*/5"   # min "*/1" (1 min); proxy is config-light
    replication_rate: 0           # MB/s cap; 0 = unlimited (fine on NVMe/LAN)
    guests: []                    # empty = manage ALL guests

  tasks:
    - name: Get cluster node list
      ansible.builtin.command: pvesh get /nodes --output-format json
      register: pve_nodes_raw
      changed_when: false
      check_mode: false

    - name: Parse node names
      ansible.builtin.set_fact:
        cluster_nodes: "{{ pve_nodes_raw.stdout | from_json | map(attribute='node') | list | sort }}"

    - name: Require at least two nodes
      ansible.builtin.assert:
        that: cluster_nodes | length >= 2
        fail_msg: "Need >=2 nodes for replicate-all; found {{ cluster_nodes }}"

    - name: Get all guests (VMs + LXCs) and their current placement
      ansible.builtin.command: pvesh get /cluster/resources --type vm --output-format json
      register: pve_resources_raw
      changed_when: false
      check_mode: false

    - name: Build guest list
      ansible.builtin.set_fact:
        all_guests: "{{ pve_resources_raw.stdout | from_json }}"

    - name: Resolve managed guest set (all, or the -e guests=[...] subset)
      ansible.builtin.set_fact:
        managed_guests: >-
          {{ all_guests | selectattr('vmid', 'in', guests) | list
             if (guests | length > 0) else all_guests }}

    - name: Show the plan
      ansible.builtin.debug:
        msg: "Nodes={{ cluster_nodes }} | guests={{ managed_guests | map(attribute='vmid') | list }}"

    # ----- Replication: every guest -> every other node ---------------------

    - name: Read existing replication jobs
      ansible.builtin.command: pvesh get /cluster/replication --output-format json
      register: repl_raw
      changed_when: false
      check_mode: false

    # The unit of truth is the (guest -> target) PAIR. Job ids are Proxmox's
    # business -- it renumbers them after migrations -- so we never key on them.
    - name: Parse existing replication (guest -> target) pairs
      ansible.builtin.set_fact:
        existing_pairs: >-
          {{ repl_raw.stdout | from_json
             | map(attribute='guest') | map('string') | list
             | zip(repl_raw.stdout | from_json | map(attribute='target') | list)
             | map('join', ':') | list }}

    - name: Compute missing (guest -> target) pairs
      ansible.builtin.set_fact:
        missing_pairs: >-
          {{ missing_pairs | default([]) + [{ 'vmid': item.0.vmid, 'target': item.1 }] }}
      loop: "{{ managed_guests | product(cluster_nodes) | list }}"
      when:
        - item.1 != item.0.node
        - ((item.0.vmid | string) ~ ':' ~ item.1) not in existing_pairs
      loop_control:
        label: "{{ item.0.vmid }} -> {{ item.1 }}"

    # Pick a free job number AT RUNTIME (live check, immune to renumbering
    # and to multiple creates for the same guest within this run).
    - name: Create missing replication jobs
      ansible.builtin.shell: |
        set -euo pipefail
        vmid="{{ item.vmid }}"
        target="{{ item.target }}"
        live_ids="$(pvesh get /cluster/replication --output-format json \
                    | tr ',' '\n' | grep -o '"id":"[^"]*"' | cut -d'"' -f4)"
        for n in $(seq 0 31); do
          id="${vmid}-${n}"
          if printf '%s\n' "$live_ids" | grep -qx "$id"; then
            continue
          fi
          pvesr create-local-job "$id" "$target" \
            --schedule "{{ replication_schedule }}" \
            {{ ('--rate ' ~ (replication_rate | string)) if (replication_rate | int) > 0 else '' }}
          echo "created ${id} (${vmid} -> ${target})"
          exit 0
        done
        echo "no free job id for guest ${vmid}" >&2
        exit 1
      args:
        executable: /bin/bash
      loop: "{{ missing_pairs | default([]) }}"
      loop_control:
        label: "{{ item.vmid }} -> {{ item.target }}"
      register: created_jobs
      changed_when: "'created' in (created_jobs.stdout | default(''))"

    # ----- HA: enroll each guest as a resource (runs anywhere by default) ----

    - name: Read existing HA resources
      ansible.builtin.command: pvesh get /cluster/ha/resources --output-format json
      register: ha_res_raw
      changed_when: false
      check_mode: false

    - name: Parse HA-managed sids
      ansible.builtin.set_fact:
        ha_sids: "{{ ha_res_raw.stdout | from_json | map(attribute='sid') | list }}"

    # ha-manager wants ct:<id> for LXCs and vm:<id> for QEMU VMs.
    # No node-affinity rule => PVE 9 lets it run on ANY node (= runs anywhere).
    - name: Enroll each guest into HA (state started)
      vars:
        sid: "{{ ((item.type == 'lxc') | ternary('ct', 'vm')) ~ ':' ~ (item.vmid | string) }}"
      ansible.builtin.command: >-
        ha-manager add {{ sid }} --state started
      loop: "{{ managed_guests }}"
      loop_control:
        label: "{{ item.type }}:{{ item.vmid }}"
      when: sid not in ha_sids

    - name: Summary
      ansible.builtin.debug:
        msg: >-
          Replicate-all enforced. nodes={{ cluster_nodes | length }},
          guests={{ managed_guests | length }},
          missing_pairs_created={{ missing_pairs | default([]) | length }}.
          Idempotence is (guest->target) pair-based; Proxmox owns job ids.
          HA resources can run on any node (PVE 9 default). Re-run to self-heal.
